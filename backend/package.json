{
  "name": "merid-context-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless context-check proxy for the Merid Chrome extension. Keeps the OpenAI key server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
